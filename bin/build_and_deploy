#!/bin/bash

# MANDATORY
#export DOCKER_FILE_PATH="${DOCKER_FILE_PATH:-.}"
#export DOCKER_IMAGE_PUSH_PREFIX="${DOCKER_IMAGE_PUSH_PREFIX}"

# Assuming build_and_push.sh is in the same directory
${SCRIPTS_PATH}/docker/docker_build_push.sh
